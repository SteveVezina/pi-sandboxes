{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the PI Sandbox desktop window.",
  "windows": ["main"],
  "permissions": ["core:default", "notification:default"]
}
