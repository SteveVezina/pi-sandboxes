[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "pi-sandbox"
version = "0.1.0"
description = "Pi Sandbox Python SDK"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
where = ["src"]
